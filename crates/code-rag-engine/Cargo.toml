[package]
name = "code-rag-engine"
version = "0.1.0"
authors = ["paulxiep"]
edition = "2024"

[dependencies]
code-rag-types = { path = "../code-rag-types" }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
