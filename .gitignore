/target
/data/*

# Historical per-milestone design docs (tracks A/B/C, shipped 2026-04..05);
# kept local-only — R.md is the pattern for tracked milestone specs.
/A[1-5].md
/B[1-5].md
/C[1-5].md
!/data/reports/
/data/reports/*.json
# Architecture reports are regenerated on every ingest/topology run
/data/reports/architecture_*.md
/cache
.env
/.fastembed_cache
/dist
/crates/code-rag-ui/dist
/models
/.code-rag

# Caravan-generated artifacts: per-target docker-compose overrides +
# synthetic Rust peer crates. Re-emitted by `caravan compile`; no need
# to commit. See ../caravan/docs/development_plan.md M2.
/infra/
/caravan.exe